# 생활환경기록보존원 1차 사이트

정적 사이트 파일입니다.

## 파일 구성
- `index.html` : 메인 페이지
- `styles.css` : 스타일
- `script.js` : 인터랙션 및 숨은 려 모드 전환
- `assets/logo-approved.png` : 승인된 잎+물결 로고

## 숨은 려 모드 진입 순서
1. 상단 메뉴 `기관소개` 클릭
2. 연혁의 `1912 기록 이관 자료 확인` 클릭
3. 메인 우측 패널의 `:|` 클릭
4. 검색창에 `1912` 입력 후 검색

## 배포 방법
1. GitHub 저장소에서 `Add file` > `Upload files`
2. 이 폴더 안의 파일들을 업로드
3. Cloudflare Pages에서 해당 저장소 연결


## 표식 수정
- `:|` 문자 대신 CSS 도형으로 제작
- 카드 오른쪽 아래에 흐릿하게 배치
- 마우스를 올릴 때만 조금 선명하게 표시


## 표식 2차 수정
- 카드 오른쪽 아래 절대배치 제거
- `분류 외 문서` 문구 바로 옆에 배치
- SVG로 점 2개와 세로선을 정확하게 중앙 정렬
- hover 툴팁 제거


## 慮 내부 기록망 2차 개편
- 표면 사이트와 내부 사이트를 완전히 분리
- 려 진입 시 공공사이트 전체 숨김
- 잎 로고 대신 `慮` 한자 인장 사용
- 좌측 내부 메뉴, 사건 기록, 삼직 보고, 괴이 분류, 회수 관리, 금록, 려경, 기록관 화면 추가
- 표면망 복귀 버튼 추가


## 내부 로고 변경
- 려 내부망 좌측 상단 로고를 흰색 붓글씨 `慮 記錄網` 로 변경
- 기존 한자 인장 박스 로고 제거
- 내부망 문구도 `記錄局`에서 `記錄網`으로 정리


## 1912 전환 연출
- 검색 결과 표시 후 약 4초간 내부망 접속 연출 진행
- 공공사이트 흔들림, 채도 저하, 노이즈, 스캔라인 적용
- 접근 계통 확인 문구를 순서대로 표시
- 내부 기록망이 어둠 속에서 서서히 나타나도록 변경


## v6 전환 연출 강화
- 1912 배경 텍스트 제거, 중앙에 `慮` 붓글씨 표식 사용
- 빠른 화면 찢김·노이즈·RGB 분리·플래시 강화
- `보안 접속 계통 확인 중` 타이핑 효과 추가
- 각 안내 문구를 순차 타이핑 방식으로 표시


## v7 한글 타이핑 방식 수정
- 전환 문구를 단순 글자 잘라내기 방식에서 한글 입력기 조합 방식으로 변경
- 예: `보안` → `ㅂ` → `보` → `보ㅇ` → `보아` → `보안` 형태로 표시
- 메인 제목과 안내 문구 타이핑 속도도 조금 더 자연스럽게 조정


## v8 전환 완성도 수정
- `慮` 로고를 먼저 단독으로 중앙 표시 후 완전히 사라지게 변경
- 로고가 사라진 뒤에만 보안 접속 타이핑 시작
- 한글 조합 타이핑 속도 향상
- 모든 문구가 끝까지 완성된 뒤 잠시 유지 후 내부망 전환
- 전환 전체 길이를 약 6초로 조정


## v9 전환 문구 정리
- 보안 접속 하단 문구를 5줄에서 4줄로 정리
- 각 문구가 가로로 붙지 않도록 한 줄씩 강제 배치
- 1~4줄이 빠르게 순차 완성되도록 타이핑 속도와 전환 타이밍 재조정


## v10 완성도 강화
- 전환 오버레이를 터미널 패널 형태로 고급화
- `慮` 표식 글리치와 노이즈·플래시를 더 공격적으로 강화
- 4줄 문구 타이핑 속도와 순차 완성 타이밍 재조정
- 내부망 진입 후 카드/모듈이 순차적으로 떠오르는 부팅 연출 추가
- 내부망 전체 배경 그리드·카드 광원·호버 효과 강화
- 좌측 상단 붓글씨 로고 크기 축소 및 텍스트 정렬 개선
